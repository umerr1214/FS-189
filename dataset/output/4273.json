{
  "language": "c++",
  "question": "Implement a recursive C++ function `fibonacci` that takes an integer `n` as input and returns the `n`-th Fibonacci number. The Fibonacci sequence starts with F(0)=0, F(1)=1. Demonstrate with an example.",
  "test_cases": [
    { "input": "0", "expected": "F(0) = 0" },
    { "input": "1", "expected": "F(1) = 1" },
    { "input": "5", "expected": "F(5) = 5" },
    { "input": "10", "expected": "F(10) = 55" },
    { "input": "-3", "expected": "F(-3) = 0" }
  ],
  "driver": "#include <iostream>\n#include <vector>\n\nlong long fibonacci(int n);\n\nint main() {\n    std::vector<int> inputs = {0, 1, 5, 10, -3};\n    for (int n : inputs) {\n        std::cout << \"F(\" << n << \") = \" << fibonacci(n) << std::endl;\n    }\n    return 0;\n}",
  "code_quality": 6,
  "correctness": 7,
  "error_type": "Robustness Issue",
  "errors": [
    "The function does not handle negative input values gracefully. For negative inputs, it returns 0, which is an incorrect representation of Fibonacci numbers for such inputs, rather than indicating an error or throwing an exception. This makes the function non-robust to invalid inputs."
  ]
}