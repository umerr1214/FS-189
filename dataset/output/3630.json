{
  "language": "c++",
  "question": "Find the minimum and maximum values in a 1D array of 10 integers. Initialize the array with some values.",
  "test_cases": [
    { "input": "", "expected": "Minimum value: 1\nMaximum value: 10\n" }
  ],
  "driver": "#include <iostream>\n#include <string>\n#include <sstream>\n#include <vector>\n\n// Forward declaration for the student's main function\n// The student's main function will be renamed to student_main\nint student_main(); \n\nint main() {\n    std::stringstream ss;\n    std::streambuf* oldCout = std::cout.rdbuf();\n    std::cout.rdbuf(ss.rdbuf());\n\n    student_main(); // Call the student's main function\n\n    std::cout.rdbuf(oldCout); // Restore original cout\n\n    std::cout << ss.str(); // Print the captured output\n    return 0;\n}",
  "code_quality": 5,
  "correctness": 4,
  "error_type": "Semantic Error",
  "errors": ["Array out-of-bounds access: The loop iterates from i=0 to i=10 (inclusive), attempting to access arr[10] which is beyond the declared size of the array (indices 0-9). This leads to undefined behavior and potential crashes or incorrect results."]
}