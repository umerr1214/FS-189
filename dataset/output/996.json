{
  "language": "c++",
  "question": "Create a `Calculator` class with overloaded `add` methods. One `add` method should take two integers, another two doubles, and a third `add` method should take three integers.",
  "test_cases": [
    { "input": "calc.add(5, 3)", "expected": "8" },
    { "input": "calc.add(5.5, 3.2)", "expected": "8.7" },
    { "input": "calc.add(1, 2, 3)", "expected": "6" }
  ],
  "driver": "#include <iostream>\n#include <sstream>\n#include <iomanip>\n\nint main() {\n    Calculator calc;\n    std::stringstream ss;\n    ss << calc.add(5, 3) << \"\\n\";\n    // Note: the driver expects 8.7, but the semantic error will cause 8 to be printed.\n    // We'll cast the result to double for comparison in the driver, but the underlying issue is the method's return type.\n    ss << static_cast<double>(calc.add(5.5, 3.2)) << \"\\n\"; \n    ss << calc.add(1, 2, 3) << \"\\n\";\n    std::cout << ss.str();\n    return 0;\n}",
  "code_quality": 5,
  "correctness": 4,
  "error_type": "Semantic Error",
  "errors": [
    "The `add(double, double)` method returns an `int`, causing truncation and loss of precision for the sum of two double values. Semantically, adding two doubles should yield a double result."
  ]
}