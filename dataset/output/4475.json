{
  "language": "c++",
  "question": "Develop a C++ program that converts a temperature from Celsius to Fahrenheit. The program should ask the user for the temperature in Celsius and display the equivalent in Fahrenheit. (Formula: F = C * 9/5 + 32)",
  "test_cases": [
    { "input": "25", "expected": "Enter temperature in Celsius: Temperature in Fahrenheit: 77.00\n" },
    { "input": "0", "expected": "Enter temperature in Celsius: Temperature in Fahrenheit: 32.00\n" },
    { "input": "10", "expected": "Enter temperature in Celsius: Temperature in Fahrenheit: 50.00\n" }
  ],
  "driver": "#include <iostream>\n#include <string>\n#include <sstream>\n#include <streambuf>\n\n// The user's code will be inserted here.\nextern int main(); // Declare main from the user's code\n\nint run_test(const std::string& input, std::string& output) {\n    std::stringstream ss_input(input);\n    std::stringstream ss_output;\n\n    // Save original cin/cout buffers\n    std::streambuf* old_cin_buf = std::cin.rdbuf();\n    std::streambuf* old_cout_buf = std::cout.rdbuf();\n\n    // Redirect cin/cout\n    std::cin.rdbuf(ss_input.rdbuf());\n    std::cout.rdbuf(ss_output.rdbuf());\n\n    // Run the user's main function\n    int return_code = -1; // Indicate not run or error\n    try {\n        return_code = main();\n    } catch (const std::exception& e) {\n        // Catch potential exceptions from main if it crashes\n        ss_output << \"Runtime Error: \" << e.what() << std::endl;\n    } catch (...) {\n        ss_output << \"Runtime Error: Unknown exception\" << std::endl;\n    }\n\n    // Restore original cin/cout buffers\n    std::cin.rdbuf(old_cin_buf);\n    std::cout.rdbuf(old_cout_buf);\n\n    output = ss_output.str();\n    return return_code;\n}\n\nint main() {\n    // This main is for the test harness itself, not the user's code.\n    // The actual test cases will be run by an external system calling run_test.\n    return 0;\n}",
  "code_quality": 4,
  "correctness": 3,
  "error_type": "Logical Error",
  "errors": ["The conversion formula uses integer division (9/5) which evaluates to 1, instead of floating-point division (9.0/5.0 or 9/5.0). This leads to an incorrect calculation where Fahrenheit is computed as Celsius * 1 + 32, producing incorrect results for most inputs (e.g., 25C results in 57F instead of 77F)."]
}