{
  "language": "c++",
  "question": "Implement a `BooleanFlag` class that encapsulates a single `bool` value. Overload the unary logical NOT operator (`operator!`) to invert the encapsulated boolean value and return the new value.",
  "test_cases": [
    { "input": "BooleanFlag flag(true); bool res = !flag; std::cout << res << flag.getValue();", "expected": "01" },
    { "input": "BooleanFlag flag(false); bool res = !flag; std::cout << res << flag.getValue();", "expected": "10" }
  ],
  "driver": "int main() {\n    BooleanFlag flag(true);\n    bool res = !flag;\n    std::cout << res;\n    std::cout << flag.getValue();\n    std::cout << \"\\n\";\n\n    BooleanFlag flag2(false);\n    res = !flag2;\n    std::cout << res;\n    std::cout << flag2.getValue();\n    std::cout << \"\\n\";\n    return 0;\n}",
  "code_quality": 6,
  "correctness": 7,
  "error_type": "Robustness Issue",
  "errors": [
    "The `operator!` method is declared `const`, which prevents it from modifying the internal `m_value`. This violates the requirement to 'invert the encapsulated boolean value' and leads to a robust issue where the flag's state remains unchanged after the operator is called, despite the expectation of modification."
  ]
}