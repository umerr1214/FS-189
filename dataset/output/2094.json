{
  "language": "c++",
  "question": "Design a `BankAccount` class. It should have a private `balance` (double). Implement public methods `deposit(double amount)` and `withdraw(double amount)`. Ensure `withdraw` prevents the balance from going negative and `deposit` prevents negative amounts.",
  "test_cases": [
    { "input": "INIT 100.50\nDEPOSIT 20.75\nGET_BALANCE", "expected": "121.25" },
    { "input": "INIT 50.99\nWITHDRAW 10.25\nGET_BALANCE", "expected": "40.74" },
    { "input": "INIT 0.00\nDEPOSIT 5.50\nWITHDRAW 2.75\nGET_BALANCE", "expected": "2.75" }
  ],
  "driver": "#include <iostream>\n#include <string>\n#include <sstream>\n#include <iomanip>\n\nint main() {\n    std::string line;\n    BankAccount account; \n\n    while (std::getline(std::cin, line)) {\n        std::stringstream ss(line);\n        std::string command;\n        ss >> command;\n\n        if (command == \"INIT\") {\n            double initialBalance;\n            ss >> initialBalance;\n            account = BankAccount(static_cast<int>(initialBalance));\n        } else if (command == \"DEPOSIT\") {\n            double amount;\n            ss >> amount;\n            account.deposit(amount);\n        } else if (command == \"WITHDRAW\") {\n            double amount;\n            ss >> amount;\n            account.withdraw(amount);\n        } else if (command == \"GET_BALANCE\") {\n            std::cout << std::fixed << std::setprecision(2) << account.getBalance() << std::endl;\n        }\n    }\n    return 0;\n}",
  "code_quality": 5,
  "correctness": 5,
  "error_type": "Semantic Error",
  "errors": ["The `balance` member variable is declared as `int` instead of `double`, leading to loss of precision for fractional amounts as specified by the problem. `deposit` and `withdraw` methods also cast `double` amounts to `int`."]
}