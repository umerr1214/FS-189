{
  "language": "c++",
  "question": "Create a `Logger` class. Its constructor should open a specified log file, and its destructor should close the file, ensuring proper resource management. Add a `logMessage(const std::string& message)` method.",
  "test_cases": [
    {
      "input": "Logger(\"test_log_2830.txt\");\nlogger.logMessage(\"Application started.\");\nlogger.logMessage(\"Processing data...\");",
      "expected": "Application started.\nProcessing data...\n"
    }
  ],
  "driver": "#include <fstream>\n#include <string>\n#include <iostream>\n#include <cstdio> // For std::remove\n\n// The Logger class would be inserted here if it were valid C++\n\nint main() {\n    std::string test_file = \"test_log_2830.txt\";\n    std::remove(test_file.c_str());\n\n    // This driver assumes the Logger class is correctly defined.\n    // Since the provided code has a syntax error, this driver will not compile with it.\n    // It's here to show how one would test a *correct* Logger.\n    try {\n        Logger logger(test_file);\n        logger.logMessage(\"Application started.\");\n        logger.logMessage(\"Processing data...\");\n        logger.logMessage(\"Application finished.\");\n    } catch (const std::exception& e) {\n        std::cerr << \"Error during logging: \" << e.what() << std::endl;\n    }\n\n    std::ifstream inFile(test_file);\n    if (inFile.is_open()) {\n        std::string line;\n        while (std::getline(inFile, line)) {\n            std::cout << line << std::endl;\n        }\n        inFile.close();\n    } else {\n        std::cout << \"Log file could not be opened for reading.\" << std::endl;\n    }\n    std::remove(test_file.c_str());\n    return 0;\n}",
  "code_quality": 1,
  "correctness": 0,
  "error_type": "Syntax Error",
  "errors": ["Missing semicolon after class definition."]
}