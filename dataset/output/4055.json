{
  "language": "c++",
  "question": "Write a C++ program that calculates the factorial of a non-negative integer entered by the user. Use a `for` loop to perform the calculation. Handle invalid input (negative numbers) by printing an error message.",
  "test_cases": [
    { "input": "0", "expected": "Factorial of 0 is 1\n" },
    { "input": "3", "expected": "Factorial of 3 is 6\n" },
    { "input": "5", "expected": "Factorial of 5 is 120\n" },
    { "input": "-2", "expected": "Error: Factorial is not defined for negative numbers.\n" }
  ],
  "driver": "int main() {\n    int num;\n    long long factorial = 1; // Corrected initialization for driver\n\n    std::cin >> num;\n\n    if (num < 0) {\n        std::cout << \"Error: Factorial is not defined for negative numbers.\" << std::endl;\n    } else {\n        for (int i = 1; i <= num; ++i) {\n            factorial *= i;\n        }\n        std::cout << \"Factorial of \" << num << \" is \" << factorial << std::endl;\n    }\n\n    return 0;\n}",
  "code_quality": 4,
  "correctness": 4,
  "error_type": "Logical Error",
  "errors": [
    "The `factorial` variable is initialized to 0 instead of 1. For any positive integer `num`, multiplying by 0 in the loop will always result in a factorial of 0, which is incorrect. The special case for `num == 0` is handled correctly, but the general calculation for `num > 0` is flawed."
  ]
}