{
  "language": "c++",
  "question": "Design a `Counter` class that keeps track of how many `Counter` objects have been created using a `static` member variable. Implement a default constructor to increment this count and a destructor to decrement it. Provide a static method `getTotalCount()` to retrieve the current number of active `Counter` objects.",
  "test_cases": [
    {
      "input": "",
      "expected": "Initial count: 0\nAfter c1 creation: 1\nAfter c2, c3 creation: 3\nAfter c2, c3 destruction: 1\nAfter c4 (dynamic) creation: 2\nAfter c4 (dynamic) destruction: 1\nFinal count: 1"
    }
  ],
  "driver": "#include <iostream>\n\n// The Counter class will be inserted here, followed by the main function.\n// The driver executes the main function and captures its standard output.\n\nint main() {\n    std::cout << \"Initial count: \" << Counter::getTotalCount() << std::endl;\n\n    Counter c1;\n    std::cout << \"After c1 creation: \" << Counter::getTotalCount() << std::endl;\n\n    {\n        Counter c2;\n        Counter c3;\n        std::cout << \"After c2, c3 creation: \" << Counter::getTotalCount() << std::endl;\n    }\n\n    std::cout << \"After c2, c3 destruction: \" << Counter::getTotalCount() << std::endl;\n\n    Counter* c4 = new Counter();\n    std::cout << \"After c4 (dynamic) creation: \" << Counter::getTotalCount() << std::endl;\n    delete c4;\n    std::cout << \"After c4 (dynamic) destruction: \" << Counter::getTotalCount() << std::endl;\n\n    std::cout << \"Final count: \" << Counter::getTotalCount() << std::endl;\n    return 0;\n}",
  "code_quality": 4,
  "correctness": 4,
  "error_type": "Logical Error",
  "errors": [
    "The destructor `~Counter()` incorrectly increments `totalCount` instead of decrementing it. This leads to the total count continuously increasing, rather than reflecting the number of active objects."
  ]
}