{
  "language": "c++",
  "question": "Find the minimum and maximum values in a 1D array of 10 integers. Initialize the array with some values.",
  "test_cases": [
    { "input": "{5, 2, 9, 1, 7, 3, 8, 4, 10, 6}", "expected": "Minimum value: 1\nMaximum value: 10" }
  ],
  "driver": "int main() {\n    int arr[10] = {5, 2, 9, 1, 7, 3, 8, 4, 10, 6};\n    int min_val;\n    int max_val;\n    min_val = arr[0];\n    for (int i = 1; i < 10; ++i) {\n        if (arr[i] < min_val) {\n            min_val = arr[i];\n        }\n    }\n    max_val = arr[0];\n    for (int i = 1; i < 10; ++i) {\n        if (arr[i] > max_val) {\n            max_val = arr[i];\n        }\n    }\n    std::cout << \"Minimum value: \" << min_val << std::endl;\n    std::cout << \"Maximum value: \" << max_val << std::endl;\n    return 0;\n}",
  "code_quality": 4,
  "correctness": 9,
  "error_type": "Readability / Efficiency Issue",
  "errors": [
    "The code iterates through the array twice (once for the minimum value and once for the maximum value). While correct, it is less efficient than finding both values in a single pass. For larger arrays, this inefficiency becomes more pronounced."
  ]
}