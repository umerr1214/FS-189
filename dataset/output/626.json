{
  "language": "c++",
  "question": "Write a function int searchData(int arr[], int size, int key) that performs binary search to find the key in the sorted array. Return the index if found, -1 otherwise.",
  "test_cases": [
    {
      "input": "arr = {1, 2, 3}, size = 3, key = 1",
      "expected": "0"
    }
  ],
  "driver": "#include <iostream>\n#include <cassert>\n#include \"626.cpp\"\n\nint main() {\n    int arr[] = {1, 2, 3};\n    // Should find 1 at index 0, but due to swapped logic, it looks in the wrong half\n    // assert(searchData(arr, 3, 1) == 0);\n    return 0;\n}",
  "code_quality": 4,
  "correctness": 3,
  "error_type": "Logical Error",
  "errors": [
    "Incorrect bound updates: 'start' and 'end' updates are swapped in the binary search logic, causing it to search the wrong half."
  ]
}
